import React from "react";

const NoDataMessage = ({ message }) => {
  return (
    <div className="text-light-200 text-2xl flex justify-center">{message}</div>
  );
};

export default NoDataMessage;
